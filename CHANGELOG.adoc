= Changelog

== 2026-09-02 — Homepage without the charts

* Rewrite homepage for cold visitors: plain pitch, Docs-first CTAs, three bets, living catalog, DevCentr sibling note — no architecture diagrams on the landing page.

== 2026-09-02 — Tool runs leave the scrollback

* News: compilers as structured Tool Runs (bus / daemon / embed library) + command channels; links to shell-architecture and HCI face.

== 2026-08-30 — Partner footer single entry points

* Site and news footers: one homepage link per org; drop OSO Docs / DevCentr Docs / GitHub org duplicates from the related strip.

== 2026-08-21

* Rewrite homepage from docs (philosophy, thesis, entrypoint honesty, DevCentr siblinghood) into plainer visitor language.
* Replace Mermaid blueprint diagrams with scene illustrations and web-sized labels.
* Fix homepage diagrams: keep `<object>` embeds (Mermaid `foreignObject`); unstick Pages deploy by giving `mmdc`/`puppeteer` `--no-sandbox` in CI.
* Harden Mermaid SVGs for Chromium XML (`<style>` CDATA + drop `height="auto"`) so diagrams stop failing with "Encoding error".
* Pages CI ships committed diagrams (`pnpm news` only) so puppeteer/chrome downloads cannot block deploys; regenerate with `pnpm diagrams` locally when `.mmd` sources change.
* Fix dark-mode CTA text contrast with dedicated button foreground/background tokens.
* Add light/dark icon toggle: follows system preference; manual override clears on the next system theme change.

== 2026-08-13

* News: `PlayTime as a shell-boundary cousin` -- argv runner lives in Dev-Centr scriptbook; stance essays on docs.opensh.org; do not move the engine mid-build.

== 2026-07-30

* Prerender architecture diagrams from Mermaid sources in `openshellorg/shell-architecture` via `mmdc` + `@openshellorg/mermaid-svg-css-vars` (`pnpm diagrams`).
* Replace hand-authored diagram SVGs; Pages workflow checks out shell-architecture for `.mmd` sources.

== 2026-07-25

* Add AsciiDoc-authored news feed (`news/posts/`) with RSS and Pages build step.
* Publish inaugural posts: Hello OpenShellOrg; Docs that match how we work.
* Extract shared `assets/site.css` for landing + news pages.
* Initial GitHub Pages site with OpenShellOrg brand assets.
